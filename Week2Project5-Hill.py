mass = input("Enter the objects mass in kilograms: ")
velocity = input("Enter the objects velocity in meters per second: ")

momentum = float(mass) * float(velocity)

print("The objects momentum is " + str(momentum))
